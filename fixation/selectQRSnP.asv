clear
matName = strcat('healthy.mat');
load(matName);
QRS = val(500:1150);
P = val(555:721);
save('QRS.mat', 'QRS', '-mat');
save('P.mat', 'P', '-mat')
k = 0;
Coef = zeros(1,
for i = 1:(length(QRS)-length(P)+1)
    QRSokno = QRS(i:length(P)+k);
    CoefCorr = corrcoef(QRSokno,P);
    k = k + 1;
end
